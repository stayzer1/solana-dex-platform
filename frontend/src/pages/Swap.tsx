import React from "react";
import { Paper, Typography } from "@mui/material";

export const Swap = () => {
    return (
        <Paper sx={{ p: 3 }}>
            <Typography variant="h5">Swap</Typography>
            {/* Здесь будет компонент свапа */}
        </Paper>
    );
};
